---
# defaults file for users

users_present: []
users_absent: []

## Users holds all non system users

# users_present:
#   # Only the name is required, everything else is optional
#   - name: foo
#   - name: bar
#     # These are all the optional attributes
#     groups:
#       - sudo
#       - ssh
#     password: "$6$r..."
#     shell: /bin/bash
#     ssh_keys:
#       - 'ssh-rsa ...'
